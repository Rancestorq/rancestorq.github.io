import React, { useState, useEffect } from "react";
import { motion, AnimatePresence } from "framer-motion";
import { Search, User, Zap, ShieldCheck, Headphones, ChevronRight, Gamepad2, Lock, RefreshCw, CheckCircle2, Menu, X } from "lucide-react";

const Navbar = () => {
  const [isScrolled, setIsScrolled] = useState(false);
  const [mobileMenuOpen, setMobileMenuOpen] = useState(false);
  useEffect(() => { const h = () => setIsScrolled(window.scrollY > 20); window.addEventListener("scroll", h); return () => window.removeEventListener("scroll", h); }, []);
  return (<nav className={`fixed top-0 w-full z-50 transition-all duration-300 ${isScrolled ? "bg-black/80 backdrop-blur-md border-b border-white/10" : "bg-transparent"}`}><div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8"><div className="flex items-center justify-between h-20"><div className="flex-shrink-0 flex items-center gap-2 cursor-pointer"><div className="w-8 h-8 bg-red-600 rounded-lg flex items-center justify-center transform rotate-3"><Gamepad2 className="text-white w-5 h-5" /></div><span className="text-2xl font-bold text-white tracking-tight">Basic<span className="text-red-600">Shop</span></span></div><div className="hidden md:block"><div className="ml-10 flex items-baseline space-x-8">{["Keşfet", "Oyunlar", "İndirimler", "Güvenlik Kodum"].map((item) => (<a key={item} href="#" className="text-gray-300 hover:text-red-500 px-3 py-2 rounded-md text-sm font-medium transition-colors relative group">{item}<span className="absolute bottom-0 left-0 w-0 h-0.5 bg-red-600 transition-all group-hover:w-full"></span></a>))}</div></div><div className="hidden md:flex items-center gap-4"><button className="text-gray-400 hover:text-white transition-colors"><Search className="w-5 h-5" /></button><div className="w-8 h-8 rounded-full bg-gradient-to-tr from-red-600 to-orange-600 p-[2px] cursor-pointer"><div className="w-full h-full rounded-full bg-zinc-900 flex items-center justify-center"><User className="w-4 h-4 text-white" /></div></div></div><div className="md:hidden"><button onClick={() => setMobileMenuOpen(!mobileMenuOpen)} className="text-gray-400 hover:text-white">{mobileMenuOpen ? <X /> : <Menu />}</button></div></div></div>{mobileMenuOpen && (<motion.div initial={{ opacity: 0, y: -20 }} animate={{ opacity: 1, y: 0 }} className="md:hidden bg-zinc-900 border-b border-white/10"><div className="px-2 pt-2 pb-3 space-y-1 sm:px-3">{["Keşfet", "Oyunlar", "İndirimler", "Güvenlik Kodum"].map((item) => (<a key={item} href="#" className="text-gray-300 hover:text-red-500 block px-3 py-2 rounded-md text-base font-medium">{item}</a>))}</div></motion.div>)}</nav>);
};

const Hero = () => {
  return (<div className="relative h-[85vh] w-full overflow-hidden flex items-center"><div className="absolute inset-0 z-0"><img src="https://images.unsplash.com/photo-1542751371-adc38448a05e?q=80&w=2070&auto=format&fit=crop" alt="Gaming Background" className="w-full h-full object-cover" /><div className="absolute inset-0 bg-gradient-to-t from-zinc-950 via-zinc-950/80 to-black/40"></div><div className="absolute inset-0 bg-gradient-to-r from-zinc-950 via-zinc-950/60 to-transparent"></div></div><div className="relative z-10 max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 w-full pt-20"><motion.div initial={{ opacity: 0, x: -50 }} animate={{ opacity: 1, x: 0 }} transition={{ duration: 0.8 }} className="max-w-3xl"><h1 className="text-5xl md:text-7xl font-extrabold text-white leading-tight mb-6">Bir sonraki <br /><span className="text-transparent bg-clip-text bg-gradient-to-r from-red-500 to-orange-600">maceran</span> burada başlıyor.</h1><p className="text-lg md:text-xl text-gray-400 mb-8 max-w-2xl">En popüler oyun hesaplarına anında erişim sağlayın. Güvenli ödeme, otomatik teslimat ve 7/24 destek ile oyun dünyasının kapılarını aralayın.</p><div className="flex flex-col sm:flex-row gap-4"><motion.button whileHover={{ scale: 1.05, boxShadow: "0 0 20px rgba(220, 38, 38, 0.5)" }} whileTap={{ scale: 0.95 }} className="px-8 py-4 bg-red-600 text-white font-bold rounded-lg flex items-center justify-center gap-2 transition-all">Oyunları Keşfet</motion.button><motion.button whileHover={{ scale: 1.05, backgroundColor: "rgba(255,255,255,0.1)" }} whileTap={{ scale: 0.95 }} className="px-8 py-4 border border-white/30 text-white font-semibold rounded-lg flex items-center justify-center gap-2 backdrop-blur-sm transition-all">Nasıl Çalışır? <ChevronRight className="w-4 h-4" /></motion.button></div></motion.div></div></div>);
};

const Features = () => {
  const features = [{ icon: Zap, title: "Anında Teslimat", desc: "Ödeme sonrası otomatik sistem." }, { icon: ShieldCheck, title: "Tam Kontrol", desc: "Hesap bilgileri size özel." }, { icon: Headphones, title: "7/24 Destek", desc: "Her zaman yanınızdayız." }];
  return (<div className="bg-zinc-950 border-b border-white/5 relative z-20 -mt-10"><div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12"><div className="grid grid-cols-1 md:grid-cols-3 gap-8">{features.map((feature, index) => (<div key={index} className="flex items-start gap-4 p-4 rounded-xl hover:bg-white/5 transition-colors group"><div className="p-3 bg-zinc-900 rounded-lg group-hover:bg-red-600/20 transition-colors"><feature.icon className="w-6 h-6 text-red-600 group-hover:text-red-500" /></div><div><h3 className="text-white font-bold text-lg">{feature.title}</h3><p className="text-gray-400 text-sm mt-1">{feature.desc}</p></div></div>))}</div></div></div>);
};

const GameCard = ({ title, image, price, onBuy }) => {
  return (<motion.div initial={{ opacity: 0, y: 20 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true }} className="group relative h-[400px] rounded-2xl overflow-hidden cursor-pointer shadow-lg shadow-black/50" onClick={onBuy}><img src={image} alt={title} className="w-full h-full object-cover transition-transform duration-700 group-hover:scale-110" /><div className="absolute inset-0 bg-gradient-to-t from-black via-black/50 to-transparent opacity-80 group-hover:opacity-90 transition-opacity"></div><div className="absolute bottom-0 left-0 w-full p-6 translate-y-4 group-hover:translate-y-0 transition-transform duration-300"><h3 className="text-2xl font-bold text-white mb-1">{title}</h3><div className="flex items-center justify-between"><span className="text-red-500 font-bold text-xl">{price} TL</span><button className="bg-red-600 text-white px-4 py-2 rounded-lg text-sm font-semibold opacity-0 group-hover:opacity-100 transition-opacity hover:bg-red-500">Hesabı Al</button></div></div></motion.div>);
};

const Storefront = ({ onGameSelect }) => {
  const games = [{ id: 1, title: "Elden Ring", price: "450", image: "https://images.unsplash.com/photo-1627856014759-08529612289d?q=80&w=1000&auto=format&fit=crop" }, { id: 2, title: "Cyberpunk 2077", price: "320", image: "https://images.unsplash.com/photo-1605901309584-818e25960b8f?q=80&w=1000&auto=format&fit=crop" }, { id: 3, title: "RDR 2", price: "280", image: "https://images.unsplash.com/photo-1542751371-adc38448a05e?q=80&w=1000&auto=format&fit=crop" }, { id: 4, title: "Hogwarts Legacy", price: "550", image: "https://images.unsplash.com/photo-1612287232817-60f8f6a520ac?q=80&w=1000&auto=format&fit=crop" }];
  return (<section className="py-20 bg-zinc-950"><div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8"><h2 className="text-3xl md:text-4xl font-bold text-white mb-12">Hangi dünyaya gitmek istersin?</h2><div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">{games.map((game) => (<GameCard key={game.id} {...game} onBuy={() => onGameSelect(game)} />))}</div></div></section>);
};

const SteamGuardModal = ({ game, onClose }) => {
  const [step, setStep] = useState("loading");
  const [guardCode, setGuardCode] = useState(null);
  const [timer, setTimer] = useState(0);
  const [inputCode, setInputCode] = useState("");
  const [isLoading, setIsLoading] = useState(false);
  const generateCode = () => { const chars = "ABCDEFGHJKLMNPQRSTUVWXYZ23456789"; let result = ""; for (let i = 0; i < 5; i++) { result += chars.charAt(Math.floor(Math.random() * chars.length)); } return result; };
  const handleRequestCode = () => { if (timer > 0) return; setIsLoading(true); setTimeout(() => { setGuardCode(generateCode()); setTimer(30); setIsLoading(false); setStep("ready"); }, 2000); };
  useEffect(() => { let interval; if (timer > 0) { interval = setInterval(() => { setTimer((prev) => prev - 1); }, 1000); } return () => clearInterval(interval); }, [timer]);
  const handleVerify = () => { if (inputCode.toUpperCase() === guardCode) { setStep("verified"); } else { alert("Hatalı kod! Lütfen tekrar deneyin."); setInputCode(""); } };
  return (<div className="fixed inset-0 z-[100] flex items-center justify-center p-4 bg-black/90 backdrop-blur-sm"><motion.div initial={{ scale: 0.9, opacity: 0 }} animate={{ scale: 1, opacity: 1 }} className="bg-zinc-900 border border-white/10 w-full max-w-md rounded-2xl overflow-hidden shadow-2xl shadow-red-900/20"><div className="p-6 border-b border-white/5 flex justify-between items-center bg-zinc-950"><div><h3 className="text-xl font-bold text-white">Hesap Erişimi</h3><p className="text-sm text-gray-400">{game?.title} • Steam</p></div><button onClick={onClose} className="text-gray-400 hover:text-white"><X /></button></div><div className="p-6 space-y-6"><div className="space-y-3 bg-black/30 p-4 rounded-lg border border-white/5"><div className="flex justify-between items-center"><span className="text-gray-400 text-sm">Kullanıcı Adı:</span><span className="text-white font-mono blur-sm select-none">user_8821</span></div><div className="flex justify-between items-center"><span className="text-gray-400 text-sm">Şifre:</span><span className="text-white font-mono blur-sm select-none">••••••••••••</span></div><div className="text-xs text-red-500 flex items-center gap-1 mt-2"><Lock className="w-3 h-3" /> Güvenlik nedeniyle bilgiler gizlenmiştir.</div></div><div className="text-center space-y-4"><div className="flex items-center justify-center gap-2 text-white font-semibold"><ShieldCheck className="text-red-600" /> Steam Guard Doğrulama</div>{step === "loading" && !guardCode ? (<div className="h-12 bg-zinc-800 rounded animate-pulse flex items-center justify-center text-gray-500 text-sm">Kod isteniyor...</div>) : step === "verified" ? (<div className="p-4 bg-green-900/20 border border-green-500/30 rounded-lg text-green-400 flex items-center justify-center gap-2"><CheckCircle2 /> Başarılı! Oturum açılıyor...</div>) : (<><div className="relative">{guardCode ? (<div className="text-3xl font-mono font-bold tracking-[0.5em] text-white bg-zinc-950 py-4 rounded border border-red-600/50 shadow-[0_0_15px_rgba(220,38,38,0.3)]">{guardCode}</div>) : (<div className="text-gray-500 text-sm py-4">Kodu görmek için aşağıdaki butona tıklayın.</div>)}</div><div className="space-y-3">{!guardCode ? (<button onClick={handleRequestCode} disabled={isLoading} className="w-full py-3 bg-red-600 hover:bg-red-700 text-white rounded font-medium transition-colors flex items-center justify-center gap-2 disabled:opacity-50">{isLoading ? <RefreshCw className="animate-spin" /> : <RefreshCw />} Yeni Kod İste</button>) : (<div className="space-y-3"><input type="text" value={inputCode} onChange={(e) => setInputCode(e.target.value)} placeholder="Kodu Girin (örn: K7Q2P)" className="w-full bg-zinc-950 border border-white/10 rounded p-3 text-white text-center uppercase tracking-widest focus:border-red-600 outline-none transition-colors" maxLength={5} /><button onClick={handleVerify} className="w-full py-3 bg-white text-black hover:bg-gray-200 rounded font-bold transition-colors">Doğrula ve Bağlan</button><div className="text-xs text-gray-500 mt-2">Yeni kod isteme süresi: <span className={timer > 0 ? "text-red-500 font-mono" : "text-green-500"}>{timer}s</span></div></div>)}</div></>)}</div></div></motion.div></div>);
};

export default function App() {
  const [selectedGame, setSelectedGame] = useState(null);
  return (<div className="min-h-screen bg-zinc-950 text-white font-sans selection:bg-red-600 selection:text-white"><Navbar /><main><Hero /><Features /><Storefront onGameSelect={setSelectedGame} /></main><footer className="bg-black py-8 border-t border-white/10 text-center text-gray-500 text-sm"><p>&copy; 2024 BasicShop. Tüm hakları saklıdır. Oyuncular için oyuncular tarafından.</p></footer><AnimatePresence>{selectedGame && (<SteamGuardModal game={selectedGame} onClose={() => setSelectedGame(null)} />)}</AnimatePresence></div>);
}
